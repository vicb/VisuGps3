- track plugin
  - load kml (!earth plugin)
  - add an overlay while loading (indicator)

- google earth plugin
  - wire earth events
  - show dialog when ge plugin is not installed / not supported

- measure plugin

- allow resizing (map & chart)

- multiple tracks

- capture slider events

- FontAwesome w/ firefox (mime type ?)

- fix compilation warnings (externs & typedef)

- export symbols

- minify CSS
